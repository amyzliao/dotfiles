to apply these settings:
1. quit arc (it overwrites file on exit)
2. `cp /path/to/StorableKeyBindings.json ~/Library/Application\ Support/Arc/StorableKeyBindings.json`
3. open arc 

this should work across Arc versions, but if Arc changes its config format, just manually re-apply shortcuts.
